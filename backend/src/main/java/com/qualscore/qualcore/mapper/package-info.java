/**
 * MapStruct mapper interfaces live here.
 * Add mappers as entities grow (e.g., LeadMapper, ConsultationMapper).
 *
 * Example:
 * <pre>
 * {@code
 * @Mapper(componentModel = "spring")
 * public interface LeadMapper {
 *     AdminLeadRecord toAdminRecord(Lead lead);
 * }
 * }
 * </pre>
 */
package com.qualscore.qualcore.mapper;
